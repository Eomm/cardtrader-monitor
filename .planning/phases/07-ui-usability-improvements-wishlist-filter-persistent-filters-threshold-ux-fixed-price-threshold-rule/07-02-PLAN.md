---
phase: 07-ui-usability-improvements
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/RuleEditor.tsx
  - src/components/CardRow.tsx
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03

must_haves:
  truths:
    - "RuleEditor on card detail page has a third 'Fixed Price' tab alongside Threshold and Stability"
    - "Fixed Price tab shows EUR amount input, direction selector, enabled toggle, and remove button"
    - "Each card row on the dashboard shows the first rule's primary value as an inline editable input"
    - "Inline input auto-saves on blur or Enter keypress with brief checkmark feedback"
    - "Inline input shows correct suffix: % for threshold, EUR for fixed price, days for stability"
    - "Direction shown as read-only emoji indicator next to the inline input"
    - "Cards with no rules show a placeholder empty state in the rule column"
  artifacts:
    - path: "src/components/RuleEditor.tsx"
      provides: "Fixed Price tab in rule editor"
      contains: "fixed_price"
    - path: "src/components/CardRow.tsx"
      provides: "Inline rule input with auto-save"
      contains: "InlineRuleInput"
  key_links:
    - from: "src/components/RuleEditor.tsx"
      to: "src/lib/cardtrader-types.ts"
      via: "import FixedPriceRule"
      pattern: "FixedPriceRule"
    - from: "src/components/CardRow.tsx"
      to: "@supabase/supabase-js"
      via: "inline save to monitored_cards"
      pattern: "supabase.*update.*notification_rule"
---

<objective>
Add a "Fixed Price" tab to the RuleEditor component and add inline rule editing to CardRow, enabling users to manage fixed-price rules from the card detail page and quickly adjust any rule's primary value directly from the dashboard.

Purpose: Complete the fixed-price rule UX (detail page management) and add the dashboard's inline editing convenience feature.

Output: Updated RuleEditor with 3 tabs, restructured CardRow with inline editable rule column.
</objective>

<execution_context>
@/Users/mspigolon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mspigolon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-usability-improvements-wishlist-filter-persistent-filters-threshold-ux-fixed-price-threshold-rule/07-CONTEXT.md
@.planning/phases/07-ui-usability-improvements-wishlist-filter-persistent-filters-threshold-ux-fixed-price-threshold-rule/07-RESEARCH.md
@.planning/phases/07-ui-usability-improvements-wishlist-filter-persistent-filters-threshold-ux-fixed-price-threshold-rule/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — FixedPriceRule type added to union -->

From src/lib/cardtrader-types.ts (after Plan 01):
```typescript
export interface FixedPriceRule {
  type: 'fixed_price';
  price_eur: number;
  direction: 'up' | 'down' | 'both';
  enabled: boolean;
}

export type NotificationRule = ThresholdRule | StabilityRule | FixedPriceRule;
```

From src/lib/cardtrader-utils.ts (after Plan 01):
```typescript
export function createDefaultFixedPriceRule(): FixedPriceRule;
```

From src/components/RuleEditor.tsx (current):
```typescript
type RuleEditorProps = {
  cardId: string;
  rules: NotificationRule[];
  onSave: () => void;
};
type ActiveTab = 'threshold' | 'stability';
// Uses: thresholdRules, stabilityRules filtered from localRules
// Has: addThresholdRule(), addStabilityRule(), updateRule(), removeRule(), handleSave()
```

From src/components/CardRow.tsx (current):
```typescript
type CardRowProps = {
  card: MonitoredCardWithPrice;
};
// Currently a <button> element wrapping entire row — MUST be restructured to <div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Fixed Price tab to RuleEditor</name>
  <files>src/components/RuleEditor.tsx</files>
  <action>
Extend RuleEditor to support the FixedPriceRule type:

1. Import `FixedPriceRule` type from `cardtrader-types.ts` and `createDefaultFixedPriceRule` from `cardtrader-utils.ts`.

2. Extend `ActiveTab` union: `type ActiveTab = 'threshold' | 'stability' | 'fixed_price';`

3. Add `fixedPriceRules` computed value: `const fixedPriceRules = localRules.filter((r): r is FixedPriceRule => r.type === 'fixed_price');`

4. Add `addFixedPriceRule()` function following the pattern of `addThresholdRule()`:
   ```
   function addFixedPriceRule() {
     setLocalRules((prev) => [...prev, createDefaultFixedPriceRule()]);
     setActiveTab('fixed_price');
     setFeedback(null);
   }
   ```

5. Add a third tab button in the tabs section: "Fixed Price ({fixedPriceRules.length})" with the same styling pattern as the other tabs.

6. Add the Fixed Price tab content panel (rendered when `activeTab === 'fixed_price'`). Mirror the threshold tab layout exactly:
   - EUR amount input: `type="number"`, `min={0.01}`, `step={0.01}`, `value={rule.price_eur}` — label shows "EUR" prefix
   - Direction selector: same 3-button group (up/down/both) as threshold tab
   - Enabled toggle: same toggle component
   - Remove button: same "Remove" link

7. The "Save Rules" button and feedback area at the bottom remain unchanged — they already save the full `localRules` array which now includes fixed_price rules.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>RuleEditor renders three tabs (Threshold, Stability, Fixed Price). Fixed Price tab shows EUR input, direction selector, enabled toggle, and remove button. Add/remove/save work correctly for fixed price rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add inline rule editing to CardRow</name>
  <files>src/components/CardRow.tsx</files>
  <action>
Restructure CardRow and add inline rule editing:

1. **Restructure outer element:** Change the outer `<button>` to a `<div role="button" tabIndex={0}>` with `onClick={() => navigate(...)}` and `onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') navigate(...); }}` for keyboard accessibility. Keep all existing Tailwind classes plus add `cursor-pointer`.

2. **Create InlineRuleInput component** (inside the same file or as a separate function). Props: `{ card: MonitoredCardWithPrice; onSaved: () => void }`.

   - Extract `firstRule` from `card.notification_rule?.[0] ?? null`.
   - If no first rule, render: `<span className="text-xs text-slate-500 italic">---</span>`.
   - Determine `primaryField` and `suffix` based on rule type:
     - `threshold`: field=`threshold_percent`, suffix=`%`
     - `fixed_price`: field=`price_eur`, suffix=`EUR`
     - `stability`: field=`range_percent`, suffix=`%`
   - Determine `directionEmoji` from first rule's direction:
     - `down`: use a down arrow (e.g., down-pointing triangle or arrow emoji)
     - `up`: use an up arrow
     - `both`: use up-down arrow
   - State: `localValue` (string), `saving` (boolean), `saved` (boolean).
   - Initialize `localValue` from first rule's primary value. For `fixed_price`, format with `.toFixed(2)`.
   - `handleSave()`: parse `localValue` to float, validate it's a number, update the notification_rule array (replace only the primary field of the first rule at index 0, keep rest unchanged), call `supabase.from('monitored_cards').update({ notification_rule: updatedRules }).eq('id', card.id)`. On success, set `saved=true` and clear after 1500ms. Use a `saving` guard to prevent double-saves from blur+Enter race.
   - Render: a `<div onClick={e => e.stopPropagation()} onKeyDown={e => e.stopPropagation()}>` wrapper containing:
     - Direction emoji (read-only span)
     - `<input type="number">` with `onBlur={handleSave}` and `onKeyDown` (Enter triggers save)
     - Suffix span
     - Checkmark span when `saved` is true

3. **Add InlineRuleInput to CardRow layout:** Insert between the name/expansion section and the price section. The row layout becomes: thumbnail | name+expansion | inline rule | price+change. Pass `card` and `onSaved` (which calls the parent's refresh callback).

4. **CardRow props update:** Add `onRuleSaved?: () => void` optional prop to `CardRowProps`. Pass through to InlineRuleInput. The DashboardPage's `fetchCards` callback will be passed down through CardList.

5. **Import supabase:** Add `import { supabase } from '../lib/supabase';` to CardRow.tsx.

6. **Styling:** The inline input should be narrow (`w-16`), use the dark palette (`bg-slate-800 border-slate-700 text-slate-100`), and not disrupt the row height. Direction emoji in `text-xs text-slate-400`. Success checkmark in `text-green-400`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>CardRow is restructured from button to div. Each card row shows an inline editable input for the first rule's primary value with correct suffix (%, EUR, or days). Auto-saves on blur/Enter with checkmark feedback. Cards without rules show "---". Clicking the input does not navigate to card detail.</done>
</task>

</tasks>

<verification>
npx tsc --noEmit
npm test
npm run dev (manual: verify RuleEditor has 3 tabs, inline editing works on dashboard)
</verification>

<success_criteria>
- RuleEditor shows Threshold, Stability, and Fixed Price tabs
- Fixed Price tab has EUR input, direction selector, enabled toggle, remove button
- CardRow shows inline rule input with type-appropriate suffix
- Auto-save on blur/Enter with checkmark feedback works
- Clicking inline input does not navigate to card detail (stopPropagation)
- Cards without rules show "---" placeholder
- TypeScript compiles without errors
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-usability-improvements-wishlist-filter-persistent-filters-threshold-ux-fixed-price-threshold-rule/07-02-SUMMARY.md`
</output>
