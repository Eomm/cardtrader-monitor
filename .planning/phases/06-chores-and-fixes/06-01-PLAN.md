---
phase: 06-chores-and-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00001_initial_schema.sql
  - supabase/migrations/00002_data_pipeline.sql
  - supabase/functions/import-wishlist/index.ts
  - scripts/fetch-prices.ts
  - scripts/sync-wishlists.ts
  - scripts/cleanup-snapshots.ts
  - src/lib/cardtrader-types.ts
  - src/lib/cardtrader-utils.ts
  - src/pages/CardDetailPage.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/SettingsPage.tsx
  - src/components/CardRow.tsx
  - src/components/RuleEditor.tsx
  - src/lib/telegram-utils.ts
  - tests/card-mapper.test.ts
  - tests/card-sorting.test.ts
  - tests/cooldown.test.ts
  - tests/telegram-utils.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "npx biome check . returns 0 errors"
    - "npm test passes with all tests green"
    - "expansion_id column in monitored_cards references ct_expansions(id) via FK"
    - "All code references expansion_id instead of expansion_name"
  artifacts:
    - path: "supabase/migrations/00001_initial_schema.sql"
      provides: "ct_expansions table + monitored_cards.expansion_id FK"
      contains: "expansion_id"
    - path: "src/lib/cardtrader-types.ts"
      provides: "Updated MonitoredCardWithPrice with expansion_id and joined name"
      contains: "expansion_id"
  key_links:
    - from: "src/pages/CardDetailPage.tsx"
      to: "supabase monitored_cards"
      via: "select with ct_expansions join"
      pattern: "ct_expansions"
    - from: "src/components/CardRow.tsx"
      to: "MonitoredCardWithPrice"
      via: "expansion_id + joined name"
      pattern: "expansion"
---

<objective>
Fix all 16 Biome lint errors and rename expansion_name to expansion_id with FK to ct_expansions.

Purpose: Clean up code quality violations and normalize the database schema before the visual overhaul in Plan 02. Since the database will be reset, migration files are modified directly.
Output: Zero Biome errors, all tests passing, schema using expansion_id with FK.
</objective>

<execution_context>
@/Users/mspigolon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mspigolon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chores-and-fixes/06-CONTEXT.md
@.planning/phases/06-chores-and-fixes/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all 16 Biome lint errors</name>
  <files>scripts/fetch-prices.ts, scripts/sync-wishlists.ts, scripts/cleanup-snapshots.ts, src/components/CardRow.tsx, src/components/RuleEditor.tsx, src/pages/DashboardPage.tsx, src/pages/SettingsPage.tsx, src/lib/telegram-utils.ts, tests/card-mapper.test.ts, tests/card-sorting.test.ts, tests/cooldown.test.ts, tests/telegram-utils.test.ts</files>
  <action>
Run `npx biome check . --write` to auto-fix all safe fixes (import ordering, formatting). Then manually fix any remaining errors:

1. **Import ordering** (most errors): biome check --write handles these automatically.
2. **Formatting** (indentation, spacing): biome check --write handles these automatically.
3. **noNonNullAssertion** in scripts/sync-wishlists.ts (~line 292): Replace `resolvedItems.get(bpId)!` with a guard: `const item = resolvedItems.get(bpId); if (!item) continue;`
4. **noNonNullAssertion** in any other files: Replace `!` assertions with null checks or optional chaining.

After auto-fix, run `npx biome check .` to verify 0 errors remain. If any persist, fix manually.

Do NOT run `--unsafe` flag -- review each remaining error manually.
  </action>
  <verify>
    <automated>npx biome check . 2>&1 | tail -5</automated>
  </verify>
  <done>npx biome check . reports 0 errors across all 55 checked files</done>
</task>

<task type="auto">
  <name>Task 2: Rename expansion_name to expansion_id with FK to ct_expansions</name>
  <files>supabase/migrations/00001_initial_schema.sql, supabase/migrations/00002_data_pipeline.sql, supabase/functions/import-wishlist/index.ts, scripts/sync-wishlists.ts, scripts/fetch-prices.ts, src/lib/cardtrader-types.ts, src/lib/cardtrader-utils.ts, src/pages/CardDetailPage.tsx, src/pages/DashboardPage.tsx, src/components/CardRow.tsx, tests/card-mapper.test.ts, tests/card-sorting.test.ts</files>
  <action>
**Database schema (DB will be reset, modify migrations directly):**

1. In `00001_initial_schema.sql`: Move `CREATE TABLE public.ct_expansions` (currently in 00002) to BEFORE the `monitored_cards` table. Change `expansion_name text` in monitored_cards to `expansion_id int REFERENCES public.ct_expansions(id)`.

2. In `00002_data_pipeline.sql`: Remove the `CREATE TABLE public.ct_expansions` block (it's now in 00001). Keep the ct_blueprints table and cache tables.

**Edge Function (import-wishlist/index.ts):**
- Where it sets `expansion_name` during card insert, change to `expansion_id` using the expansion's numeric ID (already available from the CardTrader API response -- the expansion object has an `id` field).

**Scripts:**
- `scripts/sync-wishlists.ts`: Change `expansion_name` references to `expansion_id` (numeric).
- `scripts/fetch-prices.ts`: If it references expansion_name, update. Likely only uses blueprint_id.

**TypeScript types (src/lib/cardtrader-types.ts):**
- In `MonitoredCardWithPrice` type: Change `expansion_name: string` to `expansion_id: number`. Add optional joined type: `ct_expansions?: { name: string }`.

**Utility (src/lib/cardtrader-utils.ts):**
- Update any function that references `expansion_name` to use `expansion_id`.

**Frontend queries (CardDetailPage, DashboardPage):**
- Update Supabase `.select()` calls to include the join: `.select('*, ct_expansions(name)')`.
- Access expansion name via `card.ct_expansions?.name` or `card.ct_expansions.name`.

**Components (CardRow.tsx):**
- Where it displays `expansion_name`, change to access the joined `ct_expansions.name`.

**Tests:**
- `tests/card-mapper.test.ts`: Update test data from `expansion_name: "..."` to `expansion_id: N` with appropriate mock joined data.
- `tests/card-sorting.test.ts`: Same update for test fixtures.

Run `npm test` and `npx biome check .` after all changes to verify nothing broke.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -10 && npx biome check . 2>&1 | tail -3</automated>
  </verify>
  <done>All tests pass, biome reports 0 errors, no references to expansion_name remain in codebase (except possibly comments), expansion_id is used everywhere with ct_expansions FK join for display name</done>
</task>

</tasks>

<verification>
- `npx biome check .` returns 0 errors
- `npm test` passes all tests
- `grep -r "expansion_name" src/ scripts/ supabase/ tests/` returns no hits (except comments)
- Migration 00001 contains ct_expansions table before monitored_cards
- Migration 00002 no longer creates ct_expansions
</verification>

<success_criteria>
Zero Biome lint errors. All existing tests pass with updated expansion_id references. Database schema uses expansion_id int FK to ct_expansions. Frontend components display expansion name via Supabase join.
</success_criteria>

<output>
After completion, create `.planning/phases/06-chores-and-fixes/06-01-SUMMARY.md`
</output>
