---
phase: 06-chores-and-fixes
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/pages/HowItWorksPage.tsx
  - src/pages/CardDetailPage.tsx
  - src/components/RuleEditor.tsx
  - src/App.tsx
  - supabase/functions/import-wishlist/index.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Public /how-it-works page loads without authentication"
    - "Card detail page shows wishlist name as link to CardTrader"
    - "Card detail page has Danger Zone section with Stop Monitoring"
    - "Rules section has ? icon linking to /how-it-works#rules"
    - "Importing a 3rd wishlist returns a clear error message"
  artifacts:
    - path: "src/pages/HowItWorksPage.tsx"
      provides: "Public how-it-works page with 4 content sections"
      exports: ["HowItWorksPage"]
    - path: "src/App.tsx"
      provides: "Route for /how-it-works outside ProtectedRoute"
      contains: "how-it-works"
    - path: "supabase/functions/import-wishlist/index.ts"
      provides: "Wishlist count check rejecting > 2"
      contains: "Maximum 2 wishlists"
  key_links:
    - from: "src/components/RuleEditor.tsx"
      to: "/how-it-works#rules"
      via: "anchor link on ? icon"
      pattern: "how-it-works.*rules"
    - from: "src/pages/CardDetailPage.tsx"
      to: "wishlists table"
      via: "Supabase query for wishlist name"
      pattern: "wishlist"
---

<objective>
Add the public "How it works" page, enhance the card detail page UX (wishlist link, rules help icon, danger zone), and enforce the 2-wishlist limit server-side.

Purpose: Improve discoverability and UX for existing users, provide a shareable explanation page for new users, and enforce resource limits.
Output: New public page, enhanced card detail, wishlist limit enforced.
</objective>

<execution_context>
@/Users/mspigolon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mspigolon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chores-and-fixes/06-CONTEXT.md
@.planning/phases/06-chores-and-fixes/06-RESEARCH.md
@.planning/phases/06-chores-and-fixes/06-02-SUMMARY.md

@src/App.tsx
@src/pages/CardDetailPage.tsx
@src/components/RuleEditor.tsx
@supabase/functions/import-wishlist/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public How It Works page and card detail UX enhancements</name>
  <files>src/pages/HowItWorksPage.tsx, src/App.tsx, src/pages/CardDetailPage.tsx, src/components/RuleEditor.tsx</files>
  <action>
**1. Create src/pages/HowItWorksPage.tsx:**

Build a public landing page at /how-it-works with dark modern palette (slate-900 bg, slate-100 text). Include Footer component. Content sections with `id` attributes for anchor linking:

- **Section 1 (id="setup"):** "How to setup" -- explain CardTrader API key configuration and Telegram bot chat ID setup (mention @card_trader_monitor_bot).
- **Section 2 (id="prices"):** "How price detection works" -- explain hourly price checks, baseline price comparison, cheapest-matching-offer logic.
- **Section 3 (id="rules"):** "How notification rules work" -- explain threshold mechanism (percentage from baseline, up or down), cooldown period (24h), one alert per card per run.
- **Section 4 (id="limits"):** "Current limits" -- max 2 wishlists per user, no max card limit, invite-only (not a public signup service).

Use clear headings, short paragraphs, and the dark modern palette. Add a "Back to login" or "Go to Dashboard" link at the top. Claude's discretion on layout.

**2. Add route in App.tsx:**

Add a public route (outside ProtectedRoute) for /how-it-works:
```tsx
<Route path="/how-it-works" element={<HowItWorksPage />} />
```
Place it after the login route and before the protected routes.

**3. Card detail page -- wishlist name link:**

In CardDetailPage.tsx, fetch the wishlist data to get the wishlist name and `cardtrader_wishlist_id`. The card already has `wishlist_id`. Query the wishlists table: `supabase.from('wishlists').select('name, cardtrader_wishlist_id').eq('id', card.wishlist_id).single()`. Display the wishlist name in the Properties section as a link: `https://www.cardtrader.com/wishlists/{cardtrader_wishlist_id}`.

**4. Card detail page -- Danger Zone section:**

Move the "Stop Monitoring" button from its current position to a new "Danger Zone" section at the bottom of the page. Style with a red-tinted border (border-red-500/30), subtle red background (bg-red-500/5), and a "Danger Zone" heading. Keep the existing inline confirmation pattern (confirm/cancel buttons, no browser dialog).

**5. RuleEditor -- ? help icon:**

Add a small `?` icon (use a simple circle with question mark, or an SVG) next to the "Notification Rules" heading in RuleEditor.tsx. Make it a link to `/how-it-works#rules` using react-router's Link component. Style: text-slate-400 hover:text-blue-400, small size.

Run `npm run build` and `npx biome check .` after all changes.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx biome check . 2>&1 | tail -3</automated>
  </verify>
  <done>HowItWorksPage renders at /how-it-works without auth. Card detail shows wishlist link, has danger zone, RuleEditor has ? icon linking to /how-it-works#rules. Build and lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce 2-wishlist limit in Edge Function</name>
  <files>supabase/functions/import-wishlist/index.ts</files>
  <action>
In supabase/functions/import-wishlist/index.ts, add a wishlist count check AFTER the auth/user validation step and BEFORE the import flow begins.

```typescript
// Count existing wishlists for this user
const { count, error: countError } = await supabaseAdmin
  .from('wishlists')
  .select('*', { count: 'exact', head: true })
  .eq('user_id', user.id);

if (countError) {
  return new Response(
    JSON.stringify({ error: 'Failed to check wishlist count' }),
    { status: 500, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
  );
}

if (count !== null && count >= 2) {
  return new Response(
    JSON.stringify({ error: 'Maximum 2 wishlists allowed. Remove an existing wishlist to import a new one.' }),
    { status: 400, headers: { ...corsHeaders, 'Content-Type': 'application/json' } }
  );
}
```

This is server-side validation only (per user decision). No frontend validation needed.

Run `npx biome check .` to verify no lint issues in the modified file.
  </action>
  <verify>
    <automated>npx biome check supabase/functions/import-wishlist/index.ts 2>&1 | tail -3</automated>
  </verify>
  <done>Edge Function rejects 3rd wishlist import with 400 status and clear error message. Lint clean.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npx biome check .` returns 0 errors
- /how-it-works route exists in App.tsx outside ProtectedRoute
- HowItWorksPage has all 4 sections with id attributes
- CardDetailPage displays wishlist name as link
- CardDetailPage has Danger Zone section at bottom
- RuleEditor has ? icon linking to /how-it-works#rules
- import-wishlist Edge Function checks wishlist count >= 2
</verification>

<success_criteria>
Public how-it-works page accessible without login with 4 content sections. Card detail page shows wishlist link, help icon on rules, and danger zone for stop monitoring. Edge Function rejects 3rd wishlist with clear error.
</success_criteria>

<output>
After completion, create `.planning/phases/06-chores-and-fixes/06-03-SUMMARY.md`
</output>
