---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/lib/supabase.ts
  - src/contexts/AuthContext.tsx
  - src/components/ProtectedRoute.tsx
  - src/components/Navbar.tsx
  - src/pages/LoginPage.tsx
  - src/pages/DashboardPage.tsx
  - src/pages/SettingsPage.tsx
  - src/App.tsx
  - src/main.tsx
  - src/index.css
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - SETT-01
  - SETT-02

user_setup:
  - service: supabase
    why: "Authentication and database backend"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: VITE_SUPABASE_PUBLISHABLE_DEFAULT_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon public key"
    dashboard_config:
      - task: "Create Supabase project (if not already done)"
        location: "https://supabase.com/dashboard -> New Project"
      - task: "Enable Google OAuth provider"
        location: "Supabase Dashboard -> Authentication -> Providers -> Google -> Enable"
      - task: "Set Site URL to GitHub Pages URL"
        location: "Supabase Dashboard -> Authentication -> URL Configuration -> Site URL"
      - task: "Run migration SQL in SQL Editor"
        location: "Supabase Dashboard -> SQL Editor -> paste 00001_initial_schema.sql"
      - task: "Set encryption key for API token storage"
        location: "Supabase Dashboard -> SQL Editor -> run: ALTER DATABASE postgres SET app.settings.encryption_key = 'your-secret-key';"
  - service: google-oauth
    why: "Google sign-in for users"
    env_vars: []
    dashboard_config:
      - task: "Create OAuth 2.0 Client ID"
        location: "Google Cloud Console -> APIs & Services -> Credentials -> Create OAuth client ID"
      - task: "Add Supabase callback URL as authorized redirect URI"
        location: "Google Cloud Console -> OAuth client -> Authorized redirect URIs -> Add https://<project>.supabase.co/auth/v1/callback"

must_haves:
  truths:
    - "User can click 'Sign in with Google' and complete the OAuth flow"
    - "User session persists across browser refresh (stays logged in)"
    - "User can log out from the avatar dropdown on any page"
    - "Unauthenticated users are redirected to the login page"
    - "User can add their CardTrader API token from the settings page"
    - "User can see whether a token is already saved (without seeing the actual token)"
    - "User can remove their saved token"
    - "Custom color palette is applied consistently across all pages"
  artifacts:
    - path: "src/lib/supabase.ts"
      provides: "Supabase client singleton"
      exports: ["supabase"]
    - path: "src/contexts/AuthContext.tsx"
      provides: "Auth state provider with session, user, loading, signOut"
      exports: ["AuthProvider", "useAuth"]
    - path: "src/components/ProtectedRoute.tsx"
      provides: "Route guard redirecting unauthenticated users"
      exports: ["ProtectedRoute"]
    - path: "src/components/Navbar.tsx"
      provides: "Top navbar with logo, nav links, avatar dropdown"
      exports: ["Navbar"]
    - path: "src/pages/LoginPage.tsx"
      provides: "Login page with Google OAuth button"
      exports: ["LoginPage"]
    - path: "src/pages/DashboardPage.tsx"
      provides: "Dashboard with empty state placeholder"
      exports: ["DashboardPage"]
    - path: "src/pages/SettingsPage.tsx"
      provides: "Settings page with token CRUD"
      exports: ["SettingsPage"]
    - path: "src/App.tsx"
      provides: "Router setup with all routes"
      min_lines: 30
  key_links:
    - from: "src/App.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "AuthProvider wraps entire app"
      pattern: "AuthProvider"
    - from: "src/App.tsx"
      to: "src/components/ProtectedRoute.tsx"
      via: "Dashboard and Settings routes wrapped in ProtectedRoute"
      pattern: "ProtectedRoute"
    - from: "src/pages/LoginPage.tsx"
      to: "src/lib/supabase.ts"
      via: "signInWithOAuth call"
      pattern: "signInWithOAuth.*google"
    - from: "src/components/Navbar.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "useAuth for user info and signOut"
      pattern: "useAuth"
    - from: "src/pages/SettingsPage.tsx"
      to: "src/lib/supabase.ts"
      via: "RPC calls for save_api_token, has_api_token, remove_api_token"
      pattern: "supabase\\.rpc"
---

<objective>
Build the complete auth flow (Google OAuth sign-in, session persistence, logout), the app shell (navbar, routing, protected routes), all three pages (login, dashboard, settings), and the settings page with CardTrader API token management.

Purpose: Deliver the core user experience for Phase 1 -- users can sign in, navigate the app, and manage their API token.
Output: A fully functional SPA with authentication, navigation, and settings.
</objective>

<execution_context>
@/Users/mspigolon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mspigolon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@docs/plans/2026-02-01-cardtrader-monitor-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth infrastructure, app shell, and routing</name>
  <files>src/lib/supabase.ts, src/contexts/AuthContext.tsx, src/components/ProtectedRoute.tsx, src/components/Navbar.tsx, src/pages/LoginPage.tsx, src/pages/DashboardPage.tsx, src/App.tsx, src/main.tsx, src/index.css</files>
  <action>
1. Create `src/lib/supabase.ts` -- Supabase client singleton per research Pattern 5:
   - Read `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_DEFAULT_KEY` from `import.meta.env`
   - Export `supabase` client instance
   - IMPORTANT: Single instance only (anti-pattern: multiple clients cause session sync issues)

2. Create `src/contexts/AuthContext.tsx` per research Pattern 1:
   - State: `session`, `user` (derived from session), `loading`
   - On mount: `supabase.auth.getSession()` then `onAuthStateChange` listener
   - Cleanup: unsubscribe on unmount
   - Provide `signOut` function
   - Export `AuthProvider` component and `useAuth` hook
   - NOTE: Use `getSession()` for initial load (reads localStorage, fast). For security-critical checks later, use `getUser()` (validates with server). Phase 1 only needs `getSession()`.

3. Create `src/components/ProtectedRoute.tsx` per research Pattern 2:
   - Uses `useAuth()` hook
   - If loading: show a centered spinner or "Loading..." with theme colors
   - If not authenticated: `<Navigate to="/" replace />`
   - If authenticated: render children

4. Create `src/components/Navbar.tsx`:
   - Top navbar (per user decision): logo on left, nav links (Dashboard, Settings) in center/left area, user avatar dropdown on right
   - Logo: text "CardTrader Monitor" or abbreviated "CTM" linking to /dashboard
   - Nav links: "Dashboard" and "Settings" with active state using Flag Red (`text-flag-red`)
   - Avatar dropdown (top-right per user decision): shows user email and "Log out" button
   - Implement dropdown as a simple click-toggle div with absolute positioning
   - Colors: navbar bg-deep-space (dark always) with text-papaya for contrast. Or adapt to light/dark mode -- use discretion for best visual result
   - Mobile: hamburger menu or simplified layout (Claude's discretion)

5. Create `src/pages/LoginPage.tsx`:
   - Landing page for unauthenticated users (Claude's discretion on layout)
   - "Sign in with Google" button that calls `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: window.location.origin + import.meta.env.BASE_URL } })`
   - Use Flag Red for the CTA button
   - Brief tagline/description of the app
   - If user is already authenticated, redirect to /dashboard

6. Create `src/pages/DashboardPage.tsx`:
   - Empty state placeholder (Claude's discretion on appearance)
   - Friendly message like "No cards being monitored yet" with suggestion to import a wishlist (which comes in Phase 2)
   - Use Steel Blue for secondary elements

7. Update `src/App.tsx`:
   - Wrap app in `AuthProvider`
   - Use `BrowserRouter` from `react-router` with `basename="/cardtrader-monitor"`
   - Routes:
     - `/` -- LoginPage (redirects to /dashboard if authenticated)
     - `/dashboard` -- ProtectedRoute > DashboardPage
     - `/settings` -- ProtectedRoute > SettingsPage (placeholder for now)
     - `*` -- redirect to /
   - Navbar shown on all authenticated pages (inside ProtectedRoute or conditionally based on auth state)

8. Update `src/main.tsx` to import index.css (should already exist from Plan 01).

9. Update `src/index.css` -- add any additional utility styles needed (body defaults, scrollbar, etc.). Keep custom theme from Plan 01 intact.

10. Run `npx biome check . --write` to format all new files.
  </action>
  <verify>
    <automated>cd /Users/mspigolon/workspace/_experiments/cardtrader-monitor && npx biome check . && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>App builds without errors, LoginPage renders with Google sign-in button, Navbar renders with logo + nav links + avatar dropdown, Dashboard shows empty state, ProtectedRoute redirects to login, all routes work via BrowserRouter with basename</done>
</task>

<task type="auto">
  <name>Task 2: Settings page with CardTrader API token management</name>
  <files>src/pages/SettingsPage.tsx</files>
  <action>
1. Create `src/pages/SettingsPage.tsx`:
   - On mount: call `supabase.rpc('has_api_token')` to check if token exists
   - Display token status: "Token saved" (green indicator) or "No token configured" (grey)
   - NEVER display the actual token value (it is encrypted server-side, client cannot decrypt)

   **Add/Update token form:**
   - Text input for API token (type="password" with show/hide toggle -- Claude's discretion on UX)
   - "Save Token" button calls `supabase.rpc('save_api_token', { token: inputValue })`
   - On success: show confirmation (toast or inline message), clear input, update status to "Token saved"
   - On error: show error message

   **Remove token:**
   - "Remove Token" button (only shown when token exists)
   - Styled with Molten Lava color (destructive action per user palette)
   - Confirmation before removing (simple confirm dialog or inline "Are you sure?")
   - Calls `supabase.rpc('remove_api_token')`
   - On success: update status to "No token configured"

   **UX details (Claude's discretion):**
   - Page title "Settings"
   - Section heading "CardTrader API Token"
   - Brief explanation of why the token is needed and where to find it
   - Loading states while RPC calls are in progress
   - Responsive layout

2. Wire SettingsPage into App.tsx routes (replace placeholder if one exists).

3. Run `npx biome check . --write` to format.
  </action>
  <verify>
    <automated>cd /Users/mspigolon/workspace/_experiments/cardtrader-monitor && npx biome check . && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Settings page renders with token status indicator, save form, and remove button. RPC calls wired to supabase.rpc('save_api_token'), supabase.rpc('has_api_token'), and supabase.rpc('remove_api_token'). Destructive remove action uses Molten Lava color.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify auth flow and settings page</name>
  <what-built>Complete Phase 1 application: Google OAuth sign-in, session persistence, logout, navbar with avatar dropdown, dashboard empty state, settings page with API token management. All deployed locally with custom color theme and dark mode support.</what-built>
  <how-to-verify>
    1. Run `npm run dev` in the project root
    2. Open http://localhost:5173/cardtrader-monitor/ in your browser
    3. Verify the login page renders with the custom color palette
    4. Toggle your OS dark/light mode -- verify background switches between Papaya Whip (light) and Deep Space Blue (dark)
    5. Click "Sign in with Google" -- complete OAuth flow (requires Supabase project to be configured)
    6. After sign-in: verify you land on /dashboard with empty state message
    7. Verify navbar shows: logo/app name, Dashboard link, Settings link, avatar dropdown with your email
    8. Click avatar dropdown -- verify "Log out" option appears
    9. Navigate to Settings -- verify token status shows "No token configured"
    10. Enter a test token and save -- verify status updates to "Token saved"
    11. Click remove -- verify confirmation and status reverts
    12. Refresh the page -- verify you remain logged in (session persistence)
    13. Click "Log out" -- verify you are redirected to login page
    14. Try navigating to /dashboard directly while logged out -- verify redirect to login
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx biome check .` passes
- `npx vite build` succeeds
- Login page has Google OAuth sign-in button
- Auth context provides session, user, loading, signOut
- Protected routes redirect unauthenticated users
- Navbar shows on authenticated pages with logo, nav links, avatar dropdown
- Dashboard shows empty state
- Settings page shows token status and CRUD controls
- All Supabase RPC calls wired correctly
- Custom 5-color palette used consistently
- Dark mode toggles with system preference
</verification>

<success_criteria>
- AUTH-01: Google OAuth sign-in works via Supabase
- AUTH-02: Session persists across browser refresh
- AUTH-03: Logout works from avatar dropdown on any page
- SETT-01: User can save a CardTrader API token (encrypted via RPC)
- SETT-02: User can update or remove their token
- App shell renders with navbar, routing, and protected routes
- Custom color palette applied across all pages
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
