---
phase: 03-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/cardtrader-types.ts
  - src/lib/cardtrader-utils.ts
  - supabase/migrations/00003_notification_rule_array.sql
  - supabase/functions/import-wishlist/index.ts
  - tests/notification-rule.test.ts
  - tests/flag-emoji.test.ts
  - tests/card-sorting.test.ts
autonomous: true
requirements: [FILT-02, RULE-01, RULE-02]

must_haves:
  truths:
    - "NotificationRule type supports both threshold and stability rule variants"
    - "Existing single-object notification_rule data is migrated to array format"
    - "Language codes map to correct flag emoji for display"
    - "Cards sort with active first by price ascending, inactive dimmed at bottom"
  artifacts:
    - path: "src/lib/cardtrader-types.ts"
      provides: "ThresholdRule, StabilityRule, NotificationRule union type"
      contains: "type: 'stability'"
    - path: "src/lib/cardtrader-utils.ts"
      provides: "languageToFlag, createDefaultStabilityRule, card sorting"
      exports: ["languageToFlag", "createDefaultStabilityRule", "sortCards"]
    - path: "supabase/migrations/00003_notification_rule_array.sql"
      provides: "Migration wrapping single rule objects into arrays"
    - path: "supabase/functions/import-wishlist/index.ts"
      provides: "Updated to write notification_rule as array"
    - path: "tests/flag-emoji.test.ts"
      provides: "Tests for language-to-flag mapping"
    - path: "tests/card-sorting.test.ts"
      provides: "Tests for card sorting logic"
    - path: "tests/notification-rule.test.ts"
      provides: "Updated tests for array format and stability rules"
  key_links:
    - from: "src/lib/cardtrader-types.ts"
      to: "src/lib/cardtrader-utils.ts"
      via: "import NotificationRule union type"
      pattern: "import.*NotificationRule"
    - from: "supabase/functions/import-wishlist/index.ts"
      to: "notification_rule column"
      via: "writes array instead of single object"
      pattern: "\\[.*createDefaultNotificationRule"
---

<objective>
Update type system, utilities, and database to support the Phase 3 dashboard features: JSONB array notification rules (threshold + stability), language-to-flag-emoji mapping, card sorting logic, and corresponding tests.

Purpose: Establish the type contracts and utilities that Plans 02 and 03 will consume. Migrate existing data so the new array format is consistent.
Output: Updated types, new utility functions, DB migration, updated Edge Function, and test coverage.
</objective>

<execution_context>
@/Users/mspigolon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mspigolon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard/03-CONTEXT.md
@.planning/phases/03-dashboard/03-RESEARCH.md

@src/lib/cardtrader-types.ts
@src/lib/cardtrader-utils.ts
@supabase/functions/import-wishlist/index.ts
@tests/notification-rule.test.ts

<interfaces>
<!-- Current types the executor needs to understand and modify -->

From src/lib/cardtrader-types.ts:
```typescript
export interface NotificationRule {
  type: 'threshold';
  threshold_percent: number;
  direction: 'up' | 'down' | 'both';
  enabled: boolean;
}
```

From src/components/CardItem.tsx:
```typescript
export type MonitoredCardWithPrice = {
  id: string;
  blueprint_id: number;
  card_name: string;
  expansion_name: string;
  game_id: number;
  collector_number: string | null;
  image_url: string | null;
  baseline_price_cents: number | null;
  notification_rule: Record<string, unknown> | null;
  is_active: boolean;
  created_at: string;
  latest_price_cents: number | null;
};
```

From src/lib/cardtrader-utils.ts:
```typescript
export function createDefaultNotificationRule(): NotificationRule { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update types and create utility functions with tests</name>
  <files>src/lib/cardtrader-types.ts, src/lib/cardtrader-utils.ts, tests/flag-emoji.test.ts, tests/card-sorting.test.ts, tests/notification-rule.test.ts</files>
  <behavior>
    - languageToFlag('en') returns UK flag emoji, languageToFlag('it') returns IT flag, languageToFlag('unknown') returns 'UNKNOWN'
    - languageToFlag handles all CardTrader languages: en, it, de, fr, es, pt, ja, ko, zh, ru
    - createDefaultStabilityRule() returns { type: 'stability', range_percent: 5, consecutive_days: 3, enabled: true }
    - createDefaultNotificationRules() returns array with one default threshold rule
    - sortCards: active cards sorted by price ascending (null prices at end), inactive cards at bottom
    - sortCards: inactive cards also sorted by price ascending among themselves
  </behavior>
  <action>
1. In `cardtrader-types.ts`:
   - Rename existing `NotificationRule` to `ThresholdRule` (keep same fields)
   - Add `StabilityRule` type: `{ type: 'stability'; range_percent: number; consecutive_days: number; enabled: boolean }`
   - Create union: `type NotificationRule = ThresholdRule | StabilityRule`
   - Export all three types
   - Update `MonitoredCardWithPrice` in `CardItem.tsx` to type `notification_rule` as `NotificationRule[] | null` instead of `Record<string, unknown> | null` (move type to types file)

2. In `cardtrader-utils.ts`:
   - Add `languageToFlag(lang: string): string` using LANGUAGE_TO_COUNTRY map (en->GB, it->IT, de->DE, fr->FR, es->ES, pt->PT, ja->JP, ko->KR, zh->CN, ru->RU). Fallback to uppercase text for unknown codes.
   - Add `createDefaultStabilityRule(): StabilityRule` returning { type: 'stability', range_percent: 5, consecutive_days: 3, enabled: true }
   - Add `createDefaultNotificationRules(): NotificationRule[]` returning `[createDefaultNotificationRule()]`
   - Add `sortCards(cards: MonitoredCardWithPrice[]): MonitoredCardWithPrice[]` that sorts active by price ascending (null at end), inactive at bottom
   - Keep existing `createDefaultNotificationRule()` unchanged (returns single ThresholdRule)

3. Write tests FIRST (red), then implement:
   - `tests/flag-emoji.test.ts`: test all 10 language mappings + unknown fallback
   - `tests/card-sorting.test.ts`: test active-first, price-ascending, null-prices-at-end, inactive-at-bottom
   - `tests/notification-rule.test.ts`: update existing tests, add stability rule test, add array creator test
  </action>
  <verify>
    <automated>npx vitest run tests/flag-emoji.test.ts tests/card-sorting.test.ts tests/notification-rule.test.ts</automated>
  </verify>
  <done>All three test files pass. Types exported. Utils working. MonitoredCardWithPrice moved to cardtrader-types.ts with proper notification_rule typing.</done>
</task>

<task type="auto">
  <name>Task 2: Database migration and Edge Function update</name>
  <files>supabase/migrations/00003_notification_rule_array.sql, supabase/functions/import-wishlist/index.ts</files>
  <action>
1. Create `supabase/migrations/00003_notification_rule_array.sql`:
   ```sql
   -- Wrap existing single-object notification_rule values in an array
   UPDATE public.monitored_cards
   SET notification_rule = jsonb_build_array(notification_rule)
   WHERE notification_rule IS NOT NULL
     AND jsonb_typeof(notification_rule) = 'object';
   ```

2. Update `supabase/functions/import-wishlist/index.ts`:
   - Find the `NotificationRule` interface defined locally in the Edge Function (around line 27)
   - Update the local `createDefaultNotificationRule()` function to still return a single rule object
   - Change line ~301 where `notification_rule: defaultRule` is set to `notification_rule: [defaultRule]` (wrap in array)
   - This ensures new imports write arrays, matching the migration format

Note: The Edge Function has its own inline types (cannot import from src/lib/). Only change the value assignment, not the type definition -- Deno runtime handles JSON serialization of arrays fine.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Migration file exists and correctly wraps single objects in arrays. Edge Function writes notification_rule as a JSON array for new imports. All existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Move MonitoredCardWithPrice to types file and update imports</name>
  <files>src/lib/cardtrader-types.ts, src/components/CardItem.tsx, src/components/CardList.tsx, src/pages/DashboardPage.tsx</files>
  <action>
1. Move `MonitoredCardWithPrice` type from `CardItem.tsx` to `cardtrader-types.ts`:
   - Update `notification_rule` field from `Record<string, unknown> | null` to `NotificationRule[] | null`
   - Add fields that exist in DB but are missing from the type: `language_required: string`, `condition_required: string | null`, `foil_required: boolean | null`, `only_zero: boolean`, `wishlist_id: string`
   - Export from cardtrader-types.ts

2. Update imports in consuming files:
   - `CardItem.tsx`: remove type definition, import `MonitoredCardWithPrice` from `../lib/cardtrader-types`
   - `CardList.tsx`: update import path from `./CardItem` to `../lib/cardtrader-types`
   - `DashboardPage.tsx`: update import path from `../components/CardItem` to `../lib/cardtrader-types`

3. Verify no other files import `MonitoredCardWithPrice` from the old location.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>MonitoredCardWithPrice lives in cardtrader-types.ts with all DB columns typed. All imports updated. TypeScript compiles cleanly. Tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- all tests pass (flag emoji, card sorting, notification rules, plus existing tests)
- `npx tsc --noEmit` -- no type errors
- Migration file exists at `supabase/migrations/00003_notification_rule_array.sql`
- Edge Function writes arrays for new imports
</verification>

<success_criteria>
- NotificationRule is a union type (ThresholdRule | StabilityRule) exported from cardtrader-types.ts
- MonitoredCardWithPrice includes all DB columns (language_required, condition_required, foil_required, only_zero) and notification_rule typed as NotificationRule[] | null
- languageToFlag maps 10 language codes to flag emoji with fallback
- sortCards puts active cards first by price ascending, inactive at bottom
- DB migration wraps existing single-object rules into arrays
- Edge Function writes notification_rule as array
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard/03-01-SUMMARY.md`
</output>
